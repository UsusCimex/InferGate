global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "infergate"
    metrics_path: "/metrics/prometheus"
    static_configs:
      - targets: ["gateway:8000"]
        labels:
          instance: "infergate-gateway"
