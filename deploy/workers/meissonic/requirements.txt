# Meissonic's pipeline is vendored at build time (see POST_INSTALL in
# docker-bake.hcl / docker-compose.yml). These are its Python deps:
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
sentencepiece>=0.2.0
pillow>=10.0.0
numpy>=1.26.0
